import React from "react";
import { View, Text } from "react-native";

const Produtos = () => {
  return (
    <View style={{flex: 1}}>
      <Text>Produtos</Text>
    </View>
  );
};

export default Produtos;